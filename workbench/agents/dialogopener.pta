invoke (0,0): open
  gives: request,task:2
var 2 (2,0);
python (4,0):
  from PyQt5.QtWidgets import *
  file, type = QFileDialog.getSaveFileName(window, "Save", "agents", "*.pta")
  reads: :window
  takes: request,task:2
  gives: file,task:4
var 4 (6,0);
result (8,0): open
  takes: result,task:4
know window (4,-2);
comment (0,-2):
  This Agent shows how to open a modal dialog
  without pausing the calling agent while it is open
