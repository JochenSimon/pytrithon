var 0 Params (0,0);
python (4,0):
  print(f"Forwarding parameters with index {params.index}")
  flip = choice([True, False])
  if flip:
    params.string = params.string[::-1]
  one = One(params.string[params.index])
  takes: params:0
  gives: flip,one:1
var 1 bool,One (8,0);
python (8,4):
  reads: :agent
  rev = " reversed" if flip else ""
  print(f"Agent '{agent.name}' picked selection = '{one.selection}'{rev}.")
  takes: flip,one:1
  gives: 2
flow 2 (4,4): ()
python (0,4):
  print("Creating parameters")
  params = Params(randint(0, 25), ascii_uppercase)
  takes: 2
  gives: params:0
module (4,-2):
  from random import randint, choice
  from string import ascii_uppercase
  print(agent.args)
ontology (7,-2):
  concept Params:
    slot index: int
    slot string: str
  concept One:
    slot selection: str
self (1,-2): delay: 1000
know agent (8,-2);
